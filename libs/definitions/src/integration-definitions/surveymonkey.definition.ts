import { SingleIntegrationDefinition } from '@app/definitions/single-integration.definition'

export class SurveymonkeyDefinition extends SingleIntegrationDefinition {
  integrationKey = 'surveymonkey'
  integrationVersion = '3'
  schemaUrl = null
}
